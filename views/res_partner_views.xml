<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_add_allowed_pricelists" model="ir.ui.view">
        <field name="name">res.partner.form.add.allowed.pricelists</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <group name="misc" string="Misc" position="inside">
                    <field name="allowed_pricelist_ids" widget="many2many_tags"/>
            </group>
        </field>
    </record>
</odoo>